let movies=[];
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
async function load(){movies=await(await fetch("/api/movies")).json();render()}
function card(m,featured=false){return `<article class="${featured?"featured-card":"film-card"}" onclick="openMovie(${m.id})"><div class="card-img" style="background-image:url('${m.poster||"https://images.unsplash.com/photo-1489599849927-2ee91cede3ba?auto=format&fit=crop&w=1200&q=80"}')"></div><div class="card-info"><small>${esc(m.year||"")} · ${esc(m.genre||"FILM")}</small><h3>${esc(m.title)}</h3><span>View project ↗</span></div></article>`}
function render(){const f=movies.filter(x=>x.featured).slice(0,2),other=movies.filter(x=>!x.featured);document.querySelector("#featured").innerHTML=f.map(x=>card(x,true)).join("");document.querySelector("#allFilms").innerHTML=other.map(x=>card(x)).join("")}
function openMovie(id){const m=movies.find(x=>x.id===id);if(!m)return;document.querySelector("#modalPoster").style.backgroundImage=`url('${m.poster}')`;document.querySelector("#modalInfo").innerHTML=`<div class="eyebrow">${esc(m.category)} · ${esc(m.year||"")}</div><h2>${esc(m.title)}</h2><p>${esc(m.description)}</p>${m.trailer?`<a class="outline dark" target="_blank" href="${esc(m.trailer)}">Watch trailer ↗</a>`:""}`;document.querySelector("#modal").classList.add("show")}
function closeModal(e){if(!e||e.target.id==="modal"||e.target.tagName==="BUTTON")document.querySelector("#modal").classList.remove("show")}
window.addEventListener("load",()=>{setTimeout(()=>document.querySelector(".loader").classList.add("done"),600);load()});